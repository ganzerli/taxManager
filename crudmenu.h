#include<string>

class Crudmenu {
    public:
    int switchHandler(int num);
    std::string init(std::string username);
    std::string getPath();
    int checkfolder(std::string pathname);
    private:
};